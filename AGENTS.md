# AGENTS.md — Shop SaaS 导航图

> 本文件是 AI Agent 的上下文地图，控制在 120 行以内。
> 所有详细知识住在 `docs/` 中，本文件只负责指路。

## 项目一句话

多租户电商 SaaS：商家注册即开店，管商品、处理订单、收款。
技术栈：Next.js 15 + TypeScript + PostgreSQL + Prisma + Stripe


## AI 行为准则（Karpathy Guidelines）

> 以下四条约束 AI 的思维方式，每次写代码前必须对照。
> 对某条原则拿不准时，查阅 `docs/references/karpathy-examples.md` 中的反例对比。

### 1. 先想后写
遇到歧义，列出假设并提问，不要默默猜测。如果存在更简单的做法，主动说出来。
不清楚就停下来，说明哪里不清楚，再继续。

### 2. 简单优先
只实现被要求的，不加任何"将来可能有用"的功能、抽象或配置项。
自我检验：一个资深工程师看到这段代码会觉得过度设计吗？如果是，重写。

### 3. 外科手术式修改
只动该动的那几行。不改风格、不顺手加注释、不重构无关代码。
每一行改动都必须能追溯到用户的原始需求。
发现了不相关的问题？提出来，但不要动它。

### 4. 目标驱动
把任务转化为可验证的成功标准：
- "修复 Bug" → "先写能复现 Bug 的测试，让它通过"
- "加功能" → "写测试定义完成标准，再实现"
- "重构" → "确保测试在改动前后都通过"

多步骤任务先列计划：`步骤 → 验证方式`，逐步推进。
